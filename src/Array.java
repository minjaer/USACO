import java.util.*;

public class Array {
    public static void main(String[] args) {
        Scanner in = new Scanner(System.in);
        int goal = in.nextInt();
        int value = in.nextInt();
        in.close();

        int l = 0;
        int r = 2000000000;
        while (l < r)
        {
            int mid = (int) ((1 + r)/2);
            for (value in second half)
            {
                operations += mid - value;
            }
        }
        if (operations <= k)
        {
            l = mid;
        }
        else
        {
            r = mid;
        }
    }
}
